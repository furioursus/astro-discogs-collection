import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update this to your deployed site URL (used for canonical links, sitemaps, etc.)
  site: 'https://example.com',
  output: 'static',
});
